{
  "name": "inspector-wiki",
  "version": "1.0.0",
  "description": "InspectorWiki — The free encyclopedia for property inspectors.",
  "scripts": {
    "start": "eleventy --serve",
    "dev": "eleventy --serve",
    "build": "eleventy && npx pagefind --site public --output-path public/pagefind"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-plugin-rss": "^1.2.0",
    "luxon": "^3.4.4",
    "markdown-it": "^14.0.0",
    "pagefind": "^1.1.0"
  },
  "dependencies": {
    "markdown-it-anchor": "^9.2.0"
  }
}
